name: Release - Create Tag and Release Notes

on:
  push:
    branches:
      - main

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven
      
      - name: Generate version
        id: version
        run: |
          VERSION=$(date +%Y.%m.%d)
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Generated version: $VERSION"
      
      - name: Create Release Notes
        run: |
          mkdir -p docs
          cat > docs/RELEASE_NOTES.md << 'EOF'
          # Release Notes - ${{ steps.version.outputs.version }}
          
          **Data de Release**: ${{ github.event.head_commit.timestamp }}
          **Commit SHA**: ${{ github.sha }}
          **Branch**: main
          
          ## 📝 Alterações
          
          - Build automático e deploy via GitHub Actions
          - Testes de integração completos
          - Docker e Docker Compose configurados
          - API REST CRUD completa com Swagger
          
          ## 🔗 Links
          
          - [GitHub Repository](https://github.com/${{ github.repository }})
          - [Docker Hub](https://hub.docker.com/r/${{ secrets.DOCKER_USERNAME }}/checkpoint-api)
          - [API Documentation](http://localhost:8080/swagger-ui.html)
          
          EOF
          cat docs/RELEASE_NOTES.md
      
      - name: Create GitHub Release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: v${{ steps.version.outputs.version }}
          release_name: Release ${{ steps.version.outputs.version }}
          body_path: docs/RELEASE_NOTES.md
          draft: false
          prerelease: false
      
      - name: Upload Release Notes
        uses: actions/upload-artifact@v4
        with:
          name: release-notes
          path: docs/RELEASE_NOTES.md
          retention-days: 30
